# 前端單元測試案例文件

> Generated by sa-expert | 2026-04-16
> 測試框架建議：Vitest + Vue Test Utils + @testing-library/vue
> Mock 工具建議：MSW (Mock Service Worker) 或 vitest mock

---

## 1. 飼主管理元件（OwnerForm / OwnerList）

### 1.1 OwnerForm 表單驗證

| 測試案例 ID | 測試案例描述 | 預期結果 |
|---|---|---|
| FE-OWN-001 | 所有必填欄位為空時提交表單 | 顯示 7 個必填欄位驗證錯誤：姓名、身分證字號、聯絡電話、通訊地址、緊急聯絡人、緊急聯絡人電話、緊急聯絡人與飼主關係 |
| FE-OWN-002 | 僅填寫姓名，其他必填欄位為空時提交 | 顯示 6 個必填欄位驗證錯誤（姓名不顯示錯誤） |
| FE-OWN-003 | 所有必填欄位皆填寫，不填獸醫院資訊時提交 | 表單驗證通過，呼叫 POST /api/owners |
| FE-OWN-004 | 儲值金額輸入 -100 | 顯示「儲值餘額不得小於 0」的驗證錯誤 |
| FE-OWN-005 | 儲值金額輸入 0 | 驗證通過 |
| FE-OWN-006 | 儲值金額輸入 10000 | 驗證通過 |
| FE-OWN-007 | 編輯模式載入時，表單應帶入現有飼主資料 | 各欄位值與 API 回傳一致 |
| FE-OWN-008 | 編輯模式下清空姓名欄位後提交 | 顯示姓名為必填的驗證錯誤 |

### 1.2 OwnerList 搜尋功能

| 測試案例 ID | 測試案例描述 | 預期結果 |
|---|---|---|
| FE-OWN-101 | 輸入 "王" 進行搜尋 | 呼叫 GET /api/owners?keyword=王，顯示符合的飼主 |
| FE-OWN-102 | 輸入 "0912" 進行搜尋 | 呼叫 GET /api/owners?keyword=0912 |
| FE-OWN-103 | API 回傳空陣列 | 顯示「查無資料」提示 |
| FE-OWN-104 | 搜尋欄為空 | 呼叫 GET /api/owners 取得全部飼主列表 |
| FE-OWN-105 | 列表每筆應顯示飼主姓名、電話與寵物摘要 | 驗證列表項目包含三個欄位 |

### 1.3 API 回應處理

| 測試案例 ID | 測試案例描述 | 預期結果 |
|---|---|---|
| FE-OWN-201 | POST /api/owners 回傳 201 | 顯示保存成功訊息，導向列表頁 |
| FE-OWN-202 | POST /api/owners 回傳 400（驗證錯誤） | 顯示 API 回傳的 error.message |
| FE-OWN-203 | POST /api/owners 回傳 500（伺服器錯誤） | 顯示通用錯誤訊息 |
| FE-OWN-204 | PUT /api/owners/:id 回傳 404 | 顯示「查無此飼主」訊息 |

---

## 2. 寵物管理元件（PetForm）

### 2.1 PetForm 表單驗證

| 測試案例 ID | 測試案例描述 | 預期結果 |
|---|---|---|
| FE-PET-001 | 所有必填欄位為空時提交 | 顯示名稱、種類、品種、性別、年齡、是否結育、寵物個性、身體狀態檢視、病史的驗證錯誤 |
| FE-PET-002 | 填寫基本欄位但未勾選寵物個性 | 顯示「寵物個性為必填」錯誤 |
| FE-PET-003 | 填寫基本欄位但未設定身體狀態檢視 | 顯示「身體狀態檢視為必填」錯誤 |
| FE-PET-004 | 未勾選飼主（ownerId 為空） | 顯示「寵物必須關聯到飼主」錯誤 |

### 2.2 寵物個性多選元件

| 測試案例 ID | 測試案例描述 | 預期結果 |
|---|---|---|
| FE-PET-101 | 渲染個性勾選區塊 | 顯示 8 個選項：親人、不會咬人、不會咬狗貓、容易緊張、親狗、會咬人、會咬狗貓、有攻擊性 |
| FE-PET-102 | 勾選「親人」和「容易緊張」 | 表單資料 personality 陣列應為 ["親人", "容易緊張"] |
| FE-PET-103 | 取消勾選已選項目 | personality 陣列應移除該項目 |

### 2.3 身體狀態檢視元件

| 測試案例 ID | 測試案例描述 | 預期結果 |
|---|---|---|
| FE-PET-201 | 渲染身體狀態區塊 | 顯示眼睛、耳朵、牙齒、四肢、皮膚（正常/異常）、皮毛（正常/打結/跳蚤壁蝨） |
| FE-PET-202 | 設定耳朵為「異常」、皮毛為「打結」 | physicalExamination.ears = "異常", fur = "打結" |
| FE-PET-203 | 預設值應為全部「正常」 | 所有項目初始值為正常 |

### 2.4 寵物病史多選元件

| 測試案例 ID | 測試案例描述 | 預期結果 |
|---|---|---|
| FE-PET-301 | 渲染病史區塊 | 顯示 18 個選項（含「其它」） |
| FE-PET-302 | 勾選「心臟病」和「氣喘」 | medicalHistory = ["心臟病", "氣喘"] |
| FE-PET-303 | 勾選「其它」 | 出現自由輸入欄位 |
| FE-PET-304 | 勾選「其它」並輸入「長期皮膚過敏」 | medicalHistoryOther = "長期皮膚過敏" |
| FE-PET-305 | 取消勾選「其它」 | 自由輸入欄位隱藏，medicalHistoryOther 清空 |

### 2.5 種類與性別選擇

| 測試案例 ID | 測試案例描述 | 預期結果 |
|---|---|---|
| FE-PET-401 | 種類選項 | 提供「犬」和「貓」兩個選項 |
| FE-PET-402 | 性別選項 | 提供「公」和「母」兩個選項 |
| FE-PET-403 | 結育選項 | 提供「已結育」和「未結育」兩個選項 |

---

## 3. 客戶資料填寫頁（CustomerForm）

### 3.1 表單驗證

| 測試案例 ID | 測試案例描述 | 預期結果 |
|---|---|---|
| FE-CF-001 | 飼主所有必填欄位為空時送出 | 顯示 7 個必填欄位驗證錯誤 |
| FE-CF-002 | 飼主資料完整但未填寫任何寵物 | 顯示「至少需填寫一隻寵物」錯誤 |
| FE-CF-003 | 飼主資料完整，寵物缺少名稱 | 顯示寵物名稱為必填錯誤 |
| FE-CF-004 | 所有資料完整送出 | 依序呼叫 POST /api/owners 和 POST /api/pets |

### 3.2 多隻寵物

| 測試案例 ID | 測試案例描述 | 預期結果 |
|---|---|---|
| FE-CF-101 | 點擊「新增寵物」按鈕 | 新增一組空白寵物表單 |
| FE-CF-102 | 填寫兩隻寵物後送出 | 呼叫 POST /api/pets 兩次，每次帶入相同 ownerId |

### 3.3 畫面限制

| 測試案例 ID | 測試案例描述 | 預期結果 |
|---|---|---|
| FE-CF-201 | 頁面載入時不顯示既有飼主列表 | 無飼主列表元素 |
| FE-CF-202 | 頁面不包含刪除按鈕 | 無刪除按鈕元素 |
| FE-CF-203 | 頁面不包含行事曆連結 | 無行事曆導航元素 |
| FE-CF-204 | 頁面不包含 PDF 產生按鈕 | 無 PDF 相關元素 |
| FE-CF-205 | 頁面包含「返回店家操作」按鈕 | 返回按鈕存在且可點擊 |

### 3.4 API 呼叫流程

| 測試案例 ID | 測試案例描述 | 預期結果 |
|---|---|---|
| FE-CF-301 | POST /api/owners 成功後呼叫 POST /api/pets | 第二次呼叫帶入第一次回傳的 ownerId |
| FE-CF-302 | POST /api/owners 失敗 | 不呼叫 POST /api/pets，顯示錯誤 |
| FE-CF-303 | POST /api/owners 成功但 POST /api/pets 失敗 | 顯示寵物資料保存失敗的錯誤 |

---

## 4. 行事曆元件（CalendarView）

### 4.1 月檢視

| 測試案例 ID | 測試案例描述 | 預期結果 |
|---|---|---|
| FE-CAL-001 | 月檢視載入時呼叫 API | 呼叫 GET /api/appointments?startDate=YYYY-MM-01&endDate=YYYY-MM-末日 |
| FE-CAL-002 | 有預約的日期顯示預約摘要 | 日期格內顯示預約數或寵物名稱 |
| FE-CAL-003 | 無預約的日期不顯示摘要 | 日期格為空 |
| FE-CAL-004 | 已取消預約有明確標示 | 已取消預約以刪除線或灰色顯示 |

### 4.2 週檢視

| 測試案例 ID | 測試案例描述 | 預期結果 |
|---|---|---|
| FE-CAL-101 | 切換至週檢視 | 顯示 7 天的行事曆格式 |
| FE-CAL-102 | 週檢視顯示該週預約 | 有預約的日期顯示摘要 |

### 4.3 日檢視

| 測試案例 ID | 測試案例描述 | 預期結果 |
|---|---|---|
| FE-CAL-201 | 切換至日檢視 | 顯示單日的預約詳細列表 |
| FE-CAL-202 | 日檢視呼叫 API | 呼叫 GET /api/appointments/date/YYYY-MM-DD |

### 4.4 檢視模式切換

| 測試案例 ID | 測試案例描述 | 預期結果 |
|---|---|---|
| FE-CAL-301 | 月 -> 週切換 | viewMode 變為 week，重新呼叫 API |
| FE-CAL-302 | 週 -> 日切換 | viewMode 變為 day，重新呼叫 API |
| FE-CAL-303 | 日 -> 月切換 | viewMode 變為 month，重新呼叫 API |

### 4.5 日期切換

| 測試案例 ID | 測試案例描述 | 預期結果 |
|---|---|---|
| FE-CAL-401 | 月檢視點擊下個月 | 月份 +1，重新呼叫 API |
| FE-CAL-402 | 月檢視點擊上個月 | 月份 -1，重新呼叫 API |
| FE-CAL-403 | 週檢視點擊下週 | 日期 +7，重新呼叫 API |
| FE-CAL-404 | 日檢視點擊隔天 | 日期 +1，重新呼叫 API |
| FE-CAL-405 | 跨年月份切換（12 月 -> 1 月） | 年份 +1，月份為 1 月 |

### 4.6 點擊導向

| 測試案例 ID | 測試案例描述 | 預期結果 |
|---|---|---|
| FE-CAL-501 | 月檢視點擊有預約的日期 | 導向 /appointments/daily/YYYY-MM-DD |
| FE-CAL-502 | 月檢視點擊預約摘要 | 導向 /appointments/daily/YYYY-MM-DD |

---

## 5. 當日預約列表元件（DailyAppointmentList）

| 測試案例 ID | 測試案例描述 | 預期結果 |
|---|---|---|
| FE-DAL-001 | 載入時呼叫 API | 呼叫 GET /api/appointments/date/YYYY-MM-DD |
| FE-DAL-002 | 顯示所有欄位 | 列表包含：預約時間、寵物名稱、飼主姓名、聯絡電話、預約狀態、是否已產生契約 |
| FE-DAL-003 | 依時間升序排列 | 第一筆時間小於第二筆 |
| FE-DAL-004 | 顯示日期標題 | 頁面標題為對應日期 |
| FE-DAL-005 | 點擊預約列導向美容服務資料頁 | router.push 帶入 appointmentId |
| FE-DAL-006 | 已取消預約顯示取消樣式與原因 | CSS 類名包含 cancelled，顯示取消原因 |
| FE-DAL-007 | 已產生契約顯示標示與下載連結 | 契約圖示可見，下載連結存在 |
| FE-DAL-008 | 無預約時顯示空狀態 | 顯示「當日無預約」 |
| FE-DAL-009 | 已完成預約顯示已完成狀態 | 狀態欄顯示「已完成」 |

---

## 6. 電子簽名元件（SignaturePad）

### 6.1 基本功能

| 測試案例 ID | 測試案例描述 | 預期結果 |
|---|---|---|
| FE-SIG-001 | 元件渲染 | canvas 元素存在，寬高符合預期 |
| FE-SIG-002 | 滑鼠繪製（mousedown + mousemove + mouseup） | canvas 不為空白（toDataURL 與空白 canvas 不同） |
| FE-SIG-003 | 觸控繪製（touchstart + touchmove + touchend） | canvas 不為空白 |
| FE-SIG-004 | 清除重簽按鈕 | canvas 恢復為空白 |
| FE-SIG-005 | 簽名預覽 | 預覽區域顯示縮圖 |

### 6.2 確認簽名

| 測試案例 ID | 測試案例描述 | 預期結果 |
|---|---|---|
| FE-SIG-101 | 有簽名後按確認 | canvas.toDataURL() 轉為 base64，呼叫 POST /api/contracts/:recordId/sign |
| FE-SIG-102 | 無簽名（空白 canvas）按確認 | 顯示「請先完成簽名」，不呼叫 API |
| FE-SIG-103 | API 回傳 200 | 顯示簽名完成提示 |
| FE-SIG-104 | API 回傳錯誤 | 顯示「簽名上傳失敗」，確認按鈕仍可點擊 |

### 6.3 簽名入口

| 測試案例 ID | 測試案例描述 | 預期結果 |
|---|---|---|
| FE-SIG-201 | 美容紀錄已產生契約 | 電子簽名按鈕可點擊 |
| FE-SIG-202 | 美容紀錄未產生契約 | 電子簽名按鈕禁用或隱藏，提示需先產生契約 |

---

## 7. 儲值金額計算顯示

| 測試案例 ID | 測試案例描述 | 預期結果 |
|---|---|---|
| FE-SV-001 | 儲值型客戶，餘額 5000，費用 500 | 顯示扣款 500、現金 0、餘額 4500 |
| FE-SV-002 | 儲值型客戶，餘額 300，費用 800 | 顯示扣款 300、現金 500、餘額 0 |
| FE-SV-003 | 儲值型客戶，餘額 800，費用 800 | 顯示扣款 800、現金 0、餘額 0 |
| FE-SV-004 | 儲值型客戶，餘額 0，費用 500 | 顯示扣款 0、現金 500、餘額 0 |
| FE-SV-005 | 非儲值型客戶 | 儲值區塊隱藏，現金 = 總費用 |
| FE-SV-006 | 多項美容費用（洗澡 500 + 美容 800） | 總費用 1300，計算以 1300 為基準 |
| FE-SV-007 | 費用欄位為空或 0 | 扣款 0、現金 0 |
| FE-SV-008 | 即時計算：修改費用後立即更新顯示 | 不需按保存，數值即時變動 |

---

## 8. 美容服務資料頁（GroomingRecordForm）

### 8.1 自動帶出資料

| 測試案例 ID | 測試案例描述 | 預期結果 |
|---|---|---|
| FE-GR-001 | 頁面載入時呼叫三支 API | GET appointment、GET owner、GET pet |
| FE-GR-002 | 飼主資料自動顯示 | 姓名、電話、地址、身分證字號、緊急聯絡人資訊 |
| FE-GR-003 | 寵物資料自動顯示 | 名稱、種類、品種、性別、年齡、結育狀態、個性、身體狀態、病史 |

### 8.2 美容項目

| 測試案例 ID | 測試案例描述 | 預期結果 |
|---|---|---|
| FE-GR-101 | 美容項目固定選單 | 顯示洗澡、美容、其他三個選項 |
| FE-GR-102 | 勾選洗澡並輸入費用 | services 包含 { item: "洗澡", price: 500 } |
| FE-GR-103 | 勾選多個項目 | services 包含多筆 |
| FE-GR-104 | 取消勾選項目 | 該項目從 services 移除 |

### 8.3 保存與產生契約

| 測試案例 ID | 測試案例描述 | 預期結果 |
|---|---|---|
| FE-GR-201 | 按下保存 | 呼叫 POST /api/grooming-records |
| FE-GR-202 | 保存成功後顯示產生契約按鈕 | 產生契約按鈕變為可點擊 |
| FE-GR-203 | 未選擇美容項目時按產生契約 | 顯示驗證錯誤 |
| FE-GR-204 | 產生契約成功 | 顯示成功訊息與下載連結 |
| FE-GR-205 | 顯示契約版本列表 | 呼叫 GET /api/contracts/:recordId/versions |

---

## 9. 資料備份頁（BackupPage）

| 測試案例 ID | 測試案例描述 | 預期結果 |
|---|---|---|
| FE-BK-001 | 頁面載入時呼叫備份列表 API | 呼叫 GET /api/backup/list |
| FE-BK-002 | 顯示一鍵備份按鈕 | 備份按鈕存在 |
| FE-BK-003 | 按下備份按鈕 | 呼叫 POST /api/backup |
| FE-BK-004 | 備份成功後重新載入列表 | 再次呼叫 GET /api/backup/list |
| FE-BK-005 | 備份列表為空 | 顯示「尚無備份檔案」 |
| FE-BK-006 | 備份失敗 | 顯示錯誤訊息 |

---

## 10. 通用 API Service 測試（Mock API）

| 測試案例 ID | 測試案例描述 | 預期結果 |
|---|---|---|
| FE-API-001 | API 回傳 200 成功回應 | 正確解析 response body |
| FE-API-002 | API 回傳 400 驗證錯誤 | 正確解析 error.message |
| FE-API-003 | API 回傳 404 查無資料 | 正確處理 404 狀態 |
| FE-API-004 | API 回傳 500 伺服器錯誤 | 顯示通用錯誤訊息 |
| FE-API-005 | API 請求逾時 | 顯示網路錯誤訊息 |
| FE-API-006 | API 基礎路徑統一為 /api | 所有 API 呼叫以 /api 開頭 |
